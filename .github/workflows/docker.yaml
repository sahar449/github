name: container
on: [workflow_dispatch]
jobs:
    docker-job:
        runs-on: ubuntu-latest
        container:
            image: nginx
        steps:
            - name: whoami
              run: whoami
            - name: python version
              uses: docker://python:3.8
              with:
                entrypoint: python --version